package com.vaadin.pekka.resizablecsslayout.client;

/**
 * Different locations where resize can start.
 */
public enum ResizeLocation {
    TOP_LEFT, TOP, TOP_RIGHT, RIGHT, BOTTOM_RIGHT, BOTTOM, BOTTOM_LEFT, LEFT;
}
